language: java

jdk:
  - openjdk8

script:
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install
after_success:
  - bash <(curl -s https://codecov.io/bash)
before_install:
  - chmod +x mvnw